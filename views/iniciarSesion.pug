<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Iniciar sesion en Uptask
                                
            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") E-mail:
                    input(type="text" name="email" placeholder="Tu Email" value= email)
                .campo
                    label(for="password") E-mail:
                    input(type="password" name="password" placeholder="Tu Password" value= password)
                .campo.enviar
                    input.boton(type="submit" value="Inicar Sesión")
                .campo
                    a(href="crear-cuenta") ¿Aún no tienes cuenta?
    